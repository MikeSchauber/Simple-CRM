import { DealsCheckPipe } from './deals-check.pipe';

describe('DealsCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new DealsCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
