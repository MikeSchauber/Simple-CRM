import { DashboardPipePipe } from './dashboard-contacts.pipe';

describe('DashboardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
