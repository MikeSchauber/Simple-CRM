import { DealStatusPipePipe } from './deal-status-pipe.pipe';

describe('DealStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DealStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
