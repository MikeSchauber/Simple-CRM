import { UpcomingDealPipe } from './upcoming-deal.pipe';

describe('UpcomingDealPipe', () => {
  it('create an instance', () => {
    const pipe = new UpcomingDealPipe();
    expect(pipe).toBeTruthy();
  });
});
